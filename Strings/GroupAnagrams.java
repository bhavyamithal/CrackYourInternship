package Strings;

public class GroupAnagrams {

    // better solution would be to use a hashmap
    // but uses space


    // bruteforce would be to traverse to each string and sort them all
    // if two of them are anagrams then after sorting they would become equal
    // then group them together if they are equal
}
